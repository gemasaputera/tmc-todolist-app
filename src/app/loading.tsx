import { Container, Loader } from '@mantine/core';
import React from 'react';

const LoadingPage = () => {
  return (
    <Container>
      <Loader color='blue' />
    </Container>
  );
};

export default LoadingPage;
