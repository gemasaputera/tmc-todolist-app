const defaultActiveSession = {
  key: '__currentsession__',
  defaultValue: ''
};

const dummyTokenSession = {
  key: '__tmc_token__',
  defaultValue: ''
};

const usersSession = {
  key: '__users__',
  defaultValue: ''
};

const userTodoList = {
  key: '__users_todo_list__',
  defaultValue: ''
};

export { defaultActiveSession, dummyTokenSession, usersSession, userTodoList };
